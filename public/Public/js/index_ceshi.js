App.registView("quote",new App.View({
  el:"#quote",
  model:new App.Model()
}),false);

App.registView("infonet",new App.View({
  el:"#infonet",
  model:new App.Model()
}),false);

App.registView("search",new App.View({
  el:"#search",
  model:new App.Model()
}),false);

App.registView("mystock",new App.View({
  el:"#mystock",
  model:new App.Model()
}),true);

App.registView("optional_edit",new App.View({
  el:"#optional_edit",
  model:new App.Model()
}),false);

App.registView("search",new App.View({
  el:"#search",
  model:new App.Model()
}),false);

App.registView("login",new App.View({
  el:"#login",
  model:new App.Model()
}),false);

App.registView("optional_list",new App.View({
  el:"#optional_list",
  model:new App.Model()
}),false);

      